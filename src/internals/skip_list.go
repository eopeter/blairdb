package internals

// SkipList is an ordered map LinkedList type ds with additional links for probabilistic determination when searching
// Gives O(Log N) performance instead of the O(N) for traditional array searches
type SkipList struct {
}
